export declare enum EndpointURLScheme {
    HTTP = "http",
    HTTPS = "https"
}
export interface EndpointURL {
    
    scheme: EndpointURLScheme;
    
    authority: string;
    
    path: string;
    
    isIp: boolean;
}
